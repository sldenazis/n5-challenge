apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "n5challenge.fullname" . }}-test-connection"
  labels:
    {{- include "n5challenge.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "n5challenge.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
